// Classes the Compose compiler cannot infer stability for, declared stable by hand.
//
// Everything here is a promise the compiler takes at its word, so each line needs a reason why
// the promise is true. Check with `-Pprorab.composeMetrics` whether a line is still doing anything.
//
// :core:domain has no Compose compiler on it, so its immutable models land here as screens
// start rendering them.
